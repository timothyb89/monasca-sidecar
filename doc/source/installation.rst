============
Installation
============

At the command line::

    $ pip install monasca-sidecar

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv monasca-sidecar
    $ pip install monasca-sidecar
